# MANIFEST des figures README

Provenance des images de `assets/readme/` (EPIC #5654, source 1 = extraction d'outputs de notebooks).

## rl4-bandits.png

- **Source** : notebook `rl_4_multi_armed_bandits.ipynb` (cellule 30, output 0)
- **Alt-text (FR)** : Bandits multi-bras : courbes de regret et exploration/exploitation comparées entre stratégies (epsilon-greedy, UCB, Thompson).
- **Poids** : 76.1 KB (PIL optimisé)

## rl5-mdp-qlearning.png

- **Source** : notebook `rl_5_mdp_dp_qlearning.ipynb` (cellule 22, output 0)
- **Alt-text (FR)** : MDP et Q-Learning : grille de valeurs d'action apprises par programmation dynamique et Q-Learning tabulaire.
- **Poids** : 29.8 KB (PIL optimisé)

## rl10-reward-shaping.png

- **Source** : notebook `rl_10_reward_shaping.ipynb` (cellule 8, output 0)
- **Alt-text (FR)** : Reward shaping : impact de la forme de récompense sur la vitesse et la stabilité d'apprentissage de l'agent.
- **Poids** : 49.4 KB (PIL optimisé)

## rl12-distributional.png

- **Source** : notebook `rl_12_distributional_rl.ipynb` (cellule 16, output 0)
- **Alt-text (FR)** : RL distributionnel : la valeur estimée est une distribution complète (pas juste l'espérance), capturant l'incertitude.
- **Poids** : 71.8 KB (PIL optimisé)

## rl13-curiosity.png

- **Source** : notebook `rl_13_curiosity_exploration.ipynb` (cellule 12, output 0)
- **Alt-text (FR)** : Exploration par curiosité : récompense intrinsèque mesurant la nouveauté pousse l'agent vers des états inconnus.
- **Poids** : 52.3 KB (PIL optimisé)

## rl11-pomdp.png

- **Source** : notebook `rl_11_pomdp.ipynb` (cellule 15, output 1)
- **Alt-text (FR)** : POMDP : apprentissage sous observation partielle, croyances sur l'état caché reconstruites depuis l'historique d'observations.
- **Poids** : 26.6 KB (PIL optimisé)

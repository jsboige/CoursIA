# ═══════════════════════════════════════════════════════════════════════════
# Configuration API ComfyUI Qwen - Fichier Template
# ═══════════════════════════════════════════════════════════════════════════
#
# 📋 INSTRUCTIONS D'UTILISATION :
#
# 1. Copier ce fichier en `.env` dans le même répertoire :
#    ```bash
#    cp .env.example .env
#    ```
#
# 2. Éditer le fichier `.env` et remplacer `your_token_here` par votre token réel
#
# 3. Le fichier `.env` est automatiquement ignoré par Git (sécurité)
#
# ═══════════════════════════════════════════════════════════════════════════

# 🔐 Token d'API ComfyUI Qwen
# Obtenez votre token via https://qwen-image-edit.myia.io/login
# Credentials par défaut : Username: etudiant | Password: CourIA2025!
QWEN_API_TOKEN=your_token_here

# ═══════════════════════════════════════════════════════════════════════════
# 📚 NOTES IMPORTANTES
# ═══════════════════════════════════════════════════════════════════════════
#
# ⚠️  SÉCURITÉ :
#   - ❌ Ne JAMAIS partager votre token avec d'autres personnes
#   - ❌ Ne JAMAIS commiter le fichier `.env` dans Git
#   - ✅ Le fichier `.env` est déjà dans .gitignore (protection automatique)
#   - ✅ En cas de perte du token, contactez votre enseignant
#
# 🔧 DÉPANNAGE :
#   - Si erreur "QWEN_API_TOKEN non trouvé" : vérifier que le fichier `.env` existe
#   - Si erreur "401 Unauthorized" : vérifier que le token est correct
#   - Le token doit être copié exactement (aucun espace avant/après)
#
# 💡 UTILISATION DANS LES NOTEBOOKS :
#   Les notebooks chargent automatiquement le token via python-dotenv :
#   ```python
#   from dotenv import load_dotenv
#   import os
#   
#   load_dotenv()
#   QWEN_API_TOKEN = os.getenv("QWEN_API_TOKEN")
#   ```
#
# ═══════════════════════════════════════════════════════════════════════════
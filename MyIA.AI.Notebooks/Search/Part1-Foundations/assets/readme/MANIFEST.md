# MANIFEST des figures README

Provenance des images de `assets/readme/` (EPIC #5654, source 1 = extraction d'outputs de notebooks).

> **Audit vision po-2025 c.434 + c.470 (2026-07-04, doctrine #5780)** : les 6 PNG ci-dessous ont été ouverts un par un via l'outil `Read` et comparés à leur alt-text. Verdict par figure dans le champ *Contenu réel vérifié*. Cohérence caption ↔ image = **2/6 correctement décrites (search3 / search15) + 4/6 héritage audit c.434/c.470 avec 2 corrections réelles découvertes pendant la migration c.763** : `search1-statespace.png` et `search2-bfs-dfs.png` étaient **intervertis** dans l'audit hérité c.470 (l'audit attribuait à search1 la description du contenu réel de search2, et inversement) — corrigés pendant la migration c.763 avec disclosure honnête. Voir *Contenu réel vérifié (c.434/c.470 corrigé c.763)* par figure.

> **Migration c.763 (2026-07-22, doctrine #5780, asset-side enforcement)** : ajout du champ canonique `Description visuelle` aux 6 entrées. Source = vision-QA MiniMax M3 (Read direct des PNG) + stats RGB PIL. Conformité avec le détecteur `scripts/notebook_tools/detect_manifest_field.py` (PR #7819 c.754). Le champ `Contenu réel vérifié` (audit c.434/c.470 po-2025, corrigé c.763 pour 2 entrées) reste en place comme preuve falsifiable détaillée ; le nouveau `Description visuelle` est la version canonique courte destinée à l'extraction automatique et au lecteur rapide. **6/6 figures conformes au détecteur.** Tranche-7 de la migration opportuniste après les 6 familles SymbolicLearning (c.754 #7819), Part4-Metaheuristics (c.755 #7824), SemanticWeb (c.756 #7826), ML.Net (c.757 #7830), Probas/PyMC (c.761 #7843) et Search/Part2-CSP (c.762 #7845). **Continuité partition-cœur Search** (c.762 Search/Part2-CSP → c.763 Search/Part1-Foundations = 7ᵉ+8ᵉ figures partition-cœur po-2024, cf L762-L1 ★★).

## search1-statespace.png

- **Source** : notebook `Search-1-StateSpace.ipynb` (cellule 13, output 0)
- **Description visuelle** : Figure matplotlib 2 sous-graphiques côte à côte (1200×671) titrée « Ordre d'exploration : BFS vs DFS ». **Gauche « BFS - Exploration par niveaux »** : arbre binaire de profondeur 4 (15 nœuds : 1 racine + 2 niveau 2 + 4 niveau 3 + 8 niveau 4), nœuds racine `1` en bleu ciel et nœuds descendants colorés en dégradé bleu du plus clair (racine) au plus foncé (feuilles 4, 5, 6, 7, 8), nœuds numérotés `N_2_i` dans des ellipses cyan pâle à bord bleu. **Droite « DFS - Exploration en profondeur »** : même arbre binaire structurellement identique (15 nœuds) mais avec un **chemin DFS colorié en vert foncé** partant de la racine `1` descendant vers un nœud `2` puis vers un nœud `3` (feuille droite extrême), tous les autres nœuds laissés blancs (non visités / non colorés). Stats RGB PIL : moyenne (242.63, 246.15, 243.74), std (37.09, 28.04, 37.15) — std modéré-élevé signature des couleurs saturées BFS (bleu) + DFS (vert) + fond blanc.
- **Alt-text (FR)** : Ordre d'exploration BFS vs DFS sur arbre binaire de profondeur 4 : à gauche BFS colore 8 nœuds du but (1, 2, 3, 4, 5, 6, 7, 8) en bleu par niveaux successifs (racine → feuilles), à droite DFS colore le chemin racine→2→3 en vert (3 nœuds en profondeur). Le reste de l'arbre DFS reste blanc (non visité).
- **Contenu réel vérifié (c.434/c.470 corrigé c.763)** : **Figure 2 sous-graphiques BFS vs DFS**, pas un état-espace d'aspirateur. L'audit hérité c.470 décrivait à tort un « grille 2×4 aspirateur Russell & Norvig » qui est en réalité le contenu de `search2-bfs-dfs.png` — inversion découverte pendant la migration c.763 par vision-QA MiniMax M3. **Note dette de nommage** : nom de fichier `search1-statespace.png` conservé par compat EPIC #5654 ; le PNG illustre BFS vs DFS (notebook Search-1-StateSpace.ipynb cellule 13), pas l'espace d'états lui-même.
- **Poids** : 94.8 KB (downscale max-dim 1200)

## search2-bfs-dfs.png

- **Source** : notebook `Search-2-Uninformed.ipynb` (cellule 28, output 0)
- **Description visuelle** : Figure networkx matplotlib (1200×388, fond blanc) titrée « Monde de l'aspirateur - Espace d'etats complet (8 etats) / S = sale, P = propre, *G/*D = position de l'aspirateur ». **Grille 2×4 de 8 nœuds** : 2 lignes (rangée haute + rangée basse) × 4 colonnes ; chaque nœud est un grand carré arrondi avec encodage `[Position|sale/propre]` + suffixe `*G` ou `*D` indiquant la position de l'aspirateur. **Codage couleur par catégorie** : 1 état initial orange saturé en haut-droite `[S|S]*G`, 2 états buts vert saturé `[P|P]*G` (haut-gauche) et `[P|P]*D` (bas-gauche), 5 autres états bleu pâle. **Arêtes courbes colorées** par action : Gauche (vert), Droite (orange), Aspirer (bleu clair) reliant les 8 nœuds. Légende haut-gauche (6 entrées : Etat initial / Etats buts / Autres etats / Aspirer / Gauche / Droite). Stats RGB PIL : moyenne (251.59, 251.83, 252.01), std (23.33, 21.94, 21.67) — **fond blanc quasi-pur** dominant, std faible signature de grandes surfaces unies (carrés arrondis unis) avec peu de pixels colorés sur les arêtes courbes.
- **Alt-text (FR)** : Espace d'états complet du monde de l'aspirateur (Russell & Norvig) : 8 configurations en grille 2×4, état initial orange, états buts verts, autres bleus, transitions étiquetées par action (Gauche / Droite / Aspirer).
- **Contenu réel vérifié (c.434/c.470 corrigé c.763)** : **Grille 2×4 monde de l'aspirateur Russell & Norvig**, pas un arbre binaire BFS/DFS. L'audit hérité c.434 décrivait à tort un « arbre binaire BFS vs DFS construit via build_binary_tree_graph(depth=4) » qui est en réalité le contenu de `search1-statespace.png` — inversion découverte pendant la migration c.763 par vision-QA MiniMax M3. **Note dette de nommage** : nom de fichier `search2-bfs-dfs.png` conservé par compat EPIC #5654 ; le PNG illustre l'espace d'états (notebook Search-2-Uninformed.ipynb cellule 28), pas la comparaison BFS/DFS.
- **Poids** : 57.3 KB (downscale max-dim 1200)

## search3-astar.png

- **Source** : notebook `Search-3-Informed.ipynb` (cellule 50, output 0)
- **Description visuelle** : Figure matplotlib (839×593, fond blanc) titrée « Progression A* (Heuristique: Distance Manhattan) ». **Grille 3×3 × 6 étapes** disposées en 2 rangées × 3 colonnes, chaque étape montrant l'état du 8-puzzle avec 8 tuiles bleues numérotées 1-8 + 1 case vide blanche. En-tête de chaque étape : « Etape N » + ligne « g=… h=… f=… ». Étape 1 (g=0, h=2, f=2) = état initial standard. Étape 2 (g=1, h=3, f=4) = première expansion. Étape 3 (g=1, h=3, f=4) = exploration intermédiaire. Étape 4 (g=1, h=3, f=4) = extension. Étape 5 (g=1, h=3, f=4) = transition. Étape 6 (g=2, h=2, f=4) = **état final = but 8-puzzle résolu**. Stats RGB PIL : moyenne (153.80, 202.62, 243.58), std **(95.91, 51.98, 25.80)** — **B >> G >> R avec std énorme sur R (95.91)** signature du fond bleu ciel dominant + tuiles bleues saturées + faible R (peu de rouge dans la palette, seulement texte/ombres).
- **Alt-text (FR)** : Progression A* (heuristique Manhattan) sur 8-puzzle : 6 étapes annotées (g = coût réel depuis l'état initial, h = heuristique jusqu'au but, f = g+h).
- **Contenu réel vérifié (c.434/c.470)** : grille 3×3 × 6 étapes, chaque tuile numérotée 1-8 + case vide, en-têtes `g=… h=… f=…` corrects étape par étape. Le chemin A* suit le plus court Manhattan (étape finale = but). **Alt-text cohérent avec l'image — 0 correction nécessaire.**
- **Poids** : 25.7 KB (natif)

## search11-metaheuristics.png

- **Source** : notebook `Search-11-Metaheuristics.ipynb` (cellule 33, output 0)
- **Description visuelle** : Figure matplotlib 2 panneaux côte à côte (1200×447, fond blanc) titrée « Comparaison des metaheuristiques ». **Gauche « Fitness par fonction (echelle log) »** : barplot groupé, axe Y logarithmique `[1e-7, 1e3+]`, axe X = 4 fonctions (Sphere / Rastrigin / Rosenbrock / Ackley), 4 barres par fonction (PSO bleu / ABC orange / SA vert / BRO rouge) avec valeurs annotées au-dessus de chaque barre (Sphere: 0.00 / 0.00 / 66.26 / 61.57, Rastrigin: 16.92 / 46.62 / 59.12 / 75.23, Rosenbrock: 7.59 / 135.72 / 33.09 / 7058.94, Ackley: 2.81 / 0.01 / 7.17 / 3.41). **Droite « Temps d'execution »** : barplot groupé linéaire, axe Y `[0, 1000+]` ms, axe X = 4 mêmes fonctions, 4 barres par fonction avec valeurs annotées (Sphere: 695/846/265/483 ms, Rastrigin: 414/898/262/467 ms, Rosenbrock: 433/947/315/459 ms, Ackley: 525/956/316/570 ms). Légende interne droite « Algorithme » : PSO / ABC / SA / BRO. Stats RGB PIL : moyenne (222.69, 211.89, 200.08), std (63.99, 61.86, 79.06) — **R > G > B avec std élevé sur les 3 canaux** signature des 4 couleurs saturées PSO/ABC/SA/BRO occupant ~30% de la surface (barplot dense) + fond blanc.
- **Alt-text (FR)** : Comparaison de 4 métaheuristiques (PSO / ABC / SA / BRO) sur 4 fonctions de benchmark (Sphere, Rastrigin, Rosenbrock, Ackley) — fitness (échelle log) et temps d'exécution en ms.
- **Contenu réel vérifié (c.434/c.470)** : deux sous-graphes côte-à-côte, barres groupées par fonction (4) × algorithme (4) = 16 mesures annotées. PSO = Particle Swarm Optimization ; ABC = Artificial Bee Colony ; SA = Simulated Annealing (recuit simulé) ; BRO = Brown Rush Optimization. **Note : la cellule 130 du notebook importe aussi `GA` (Genetic Algorithm) et `GWO`/`WOA`/`DE`, mais la figure cell.33 n'en montre que 4.** **Alt-text cohérent avec l'image — 0 correction nécessaire.**
- **Poids** : 78.4 KB (downscale max-dim 1200)

## search11-convergence.png

- **Source** : notebook `Search-11-Metaheuristics.ipynb` (cellule 40, output 0)
- **Description visuelle** : Figure matplotlib 2 panneaux côte à côte (1200×427, fond blanc quasi-pur) titrée « Analyse du parametre pop_size (PSO) ». **Gauche « Impact de pop_size sur la qualite »** : courbe verte pleine avec marqueurs ronds verts, axe X `Taille de population` ∈ [10, 100], axe Y `Fitness final` ∈ [70, 92.5]. **V-shape prononcé** : 91.8 → 82.6 → **71.7 (optimum pop_size=50)** → 85.6. **Droite « Impact de pop_size sur le temps »** : courbe bleue pleine avec marqueurs ronds bleus, mêmes axes X (taille population), axe Y `Temps (ms)` ∈ [100, 600]. **Croissance quasi-linéaire monotone** : 95 ms (pop=10) → 245 ms (pop=30) → 340 ms (pop=50) → 605 ms (pop=100). Pas de légende. Stats RGB PIL : moyenne (247.53, 248.30, 248.24), std (35.45, 32.74, 33.49) — **fond blanc quasi-pur** dominant (std modéré), std légèrement plus haut sur R signature de la courbe verte (qui tire R vers le haut) + courbe bleue (qui tire B vers le haut) mais sur ~5% de la surface seulement.
- **Alt-text (FR)** : Impact du paramètre `pop_size` sur PSO : fitness final (V-shape : 91.8 → 71.7 → 85.6, optimum ≈ pop_size 50) et temps d'exécution (croissance linéaire 95 → 605 ms).
- **Contenu réel vérifié (c.434/c.470)** : deux sous-graphes côte-à-côte, X = taille de population (10/30/50/100), Y = fitness final (gauche, échelle non-log) ou temps en ms (droite, linéaire). Le V-shape de fitness trahit un compromis exploration/exploitation propre à PSO sur la fonction testée. **Alt-text cohérent avec l'image — 0 correction nécessaire.**
- **Poids** : 81.6 KB (downscale max-dim 1200)

## search15-networkx.png

- **Source** : notebook `Search-15-NetworkX.ipynb` (cellule 30, output 0)
- **Description visuelle** : Figure networkx matplotlib (989×990, fond blanc quasi-pur) titrée « Réseau routier 5×5 — chemin optimal en rouge (Dijkstra = A*) ». **Grille 5×5 de 25 nœuds** + arêtes horizontales/verticales/diagonales formant un réseau routier pondéré. Nœuds **cercles bleus pâle uniformes** (~25 nœuds), **nœud source en haut-gauche (cercle vert foncé)**, **nœud but en bas-droite (cercle rouge saturé)**. **Arêtes colorées par gradient de poids** : jaune pâle (poids faibles) → orange (poids moyens) → rouge foncé (poids élevés). **Chemin optimal Dijkstra superposé en rouge vif** : descend en diagonale du nœud source vers le nœud but via plusieurs arêtes rouges surdimensionnées traversant la grille. Stats RGB PIL : moyenne (252.73, 250.13, 248.56), std (16.55, 27.51, 32.96) — **fond blanc quasi-pur** dominant (moyennes >248), std modéré signature des arêtes colorées + nœuds source/but en couleurs saturées sur ~10% de la surface.
- **Alt-text (FR)** : Réseau routier 5×5 nœuds-arêtes pondérées : plus court chemin optimal en rouge (Dijkstra = A* sur ce graphe à poids positifs, annoncé dans le titre de la figure).
- **Contenu réel vérifié (c.434/c.470)** : grille 5×5 de 25 nœuds bleus, arêtes colorées par gradient de poids (jaune pâle → orange → rouge selon le coût), nœud source en haut-gauche (vert), nœud but en bas-droite (rouge), chemin optimal Dijkstra superposé en rouge vif. Titre intégré à la figure : « Dijkstra = A* ». **Alt-text cohérent avec l'image — 0 correction nécessaire.**
- **Poids** : 72.8 KB (natif)

- name: "Search-05 GeneticAlgorithms"
  family: Search/Part1-Foundations
  python: MyIA.AI.Notebooks/Search/Part1-Foundations/Search-05-GeneticAlgorithms.ipynb
  csharp: MyIA.AI.Notebooks/Search/Part1-Foundations/Search-05-GeneticAlgorithms-Csharp.ipynb
  parity_level: native-both
  bridge_verdict: SOTA-OK
  bridge_verdict_reason: "Python = `deap` (Distributed Evolutionary Algorithms in Python, Fortin et al. 2012, MIT-license, lib SOTA GA Python : toolbox + creator + base + fitness maximization via `creator.FitnessMax` + algorithms.eaSimple / eaMuPlusLambda) + `pygad` (Ahmed Gad, MIT-license, GA Python SOTA-tier, interface haut-niveau `pygad.GA(...)` + run()). C# = `MetaGeneticSharp` local (wrapper .NET 9 sur GeneticSharp, MIT-license, 4 DLLs : GeneticSharp.Domain.dll + GeneticSharp.Infrastructure.Framework.dll + MetaGeneticSharp.Domain.dll + GeneticSharp.Infrastructure.dll) + `GeneticSharp` NuGet (dbarbosettig, MIT-license, lib SOTA GA .NET). Verdict SOTA-OK (modèle owner PR #10420 MERGED pour Search-11 tranche 2 + PR #10523 c.215 pour Sudoku-03 Genetic) : les DEUX cotes utilisent une vraie lib GA SOTA (deap+pygad cote PY, GeneticSharp+MetaGeneticSharp cote C#), couverture fonctionnelle equivalente (population / fitness / selection / crossover / mutation / elitism), bridge direct sans glue non-SOTA. Asymetrie documentee : PY a 2 libs (deap bas-niveau + pygad haut-niveau) vs C# 2 DLLs (GeneticSharp base + MetaGeneticSharp wrapper), l'API surface est differente (deap toolbox vs GeneticSharp fluent) mais le potentiel est equivalent."
  known_differences:
    - "Rebaseline c.34 (ai-01) : #8697 a insere le bloc `metadata.cost` (#8056) a l'identique sur les DEUX jumeaux — les deux blob SHA ont bouge, la parite semantique est inchangee. Attestation de parite, pas reparation."
    - "Socle pedagogique commun : algorithmes genetiques (AG) — composants d'un AG (population, fonction de fitness, selection (roulette/tournoi), crossover, mutation), schemas d'evolution, probleme OneMax. Les deux twins implementent le cycle evolutionnaire from-scratch (population initiale → evaluation fitness → selection → croisement → mutation → nouvelle generation)."
    - "Idiomes framework : Python = demos riches multi-frameworks (76 cellules) : AG from-scratch (OneMax) + framework **DEAP** + framework **PyGAD** (optimisation continue) + concepts avances + comparaison quantitative GA-vs-solveurs-exacts. C# = pur System.* (System / System.Collections.Generic / System.Linq), 0 NuGet, 5 using, 25 cellules (AG from-scratch compact, presentation tabulaire)."
    - "Asymetrie pedagogique : Python couvre 3 frameworks (from-scratch + DEAP + PyGAD) + comparaison vs solveurs exacts (large spectre d'outils) ; le C# demontre le cycle AG canonique en BCL pure sans dependance externe. Meme socle theorique (algorithmes evolutionnaires, Holland 1975 / Goldberg 1989), leviers de demonstration differents."
    - "Tranche 2 #10382 (2026-08-11, po-2025:CoursIA-2) : bucket-1 .NET natif via MetaGeneticSharp+GeneticSharp (submodule heberge). Verdict SOTA-OK Prong A (moteur reel branche) + Prong B DIVERGENT (16/20 vs 20/20 sur OneMax) : le couple (FloatingPointChromosome + UniformMutation) stagne a 16/20 quelque soit MutationProb ∈ {0.01, 0.05, 0.1, 0.2}. FlipBitMutation necessite IBinaryChromosome, non implemente par FloatingPointChromosome — limitation intrinseque de l'appariement, pas un defaut du moteur. Discrimination honnete documentee dans le notebook (cellules 18-19)."

- name: "CSP-2 Consistency"
  family: Search/Part2-CSP
  python: MyIA.AI.Notebooks/Search/Part2-CSP/CSP-2-Consistency.ipynb
  csharp: MyIA.AI.Notebooks/Search/Part2-CSP/CSP-2-Consistency-Csharp.ipynb
  parity_level: semantic
  bridge_verdict: SOTA-OK
  bridge_verdict_reason: "Les DEUX jumeaux atteignent le meme moteur de production Choco (parite lib-vs-lib stricte, mandat owner #10382 'lib vs lib c'est encore mieux'). C# = Choco-solver 4.10.17 (Apache-2.0) via IKVM 8.15.0 home shade DLL (recette #4711) : propagation AC-3 + fixpoint + bench AC-3 vs backtracking + coloration Australie. Python = socle from-scratch conserve (AC-3 sur `collections.deque`, forward checking, MAC — c'est le choix pedagogique du notebook : implementer AC-3 a la main) + section 3.6 ajoutee 2026-08-19 (#10382) : Choco via **pychoco** (binding Python officiel, PyPI MIT), meme modele Australie (7 IntVar 1..3, `arithm !=` sur adjacences, `WA = 1`) rejoue en un `solve()` — propagation integree du solveur observee cote a cote avec l'AC-3 maison de la cellule precedente. Solution unique valide des deux cotes. Viz : Python = matplotlib + networkx, C# = Console.WriteLine. `parity_level: semantic` preserve."
  known_differences:
    - "Edition 2026-08-19 (myia-po-2025:CoursIA-2, #10382 tranche lib-vs-lib) : section 3.6 ajoutee au jumeau Python -- propagation native Choco via pychoco (binding officiel PyPI MIT), miroir de la section 4 du jumeau C# (AC-3 avec Choco via IKVM). Meme modele Australie (7 IntVar 1..3, arithm '!=' sur adjacences, WA=1) : solve() retourne une solution valide des deux cotes. L'assignation exacte des variables non-fixees peut differer entre jumeaux et vs l'AC-3 maison (heuristique de recherche par defaut du solveur) -- documente dans la cellule d'interpretation du notebook. Temps de resolution en prose = descripteurs machine-dep renvoyant aux sorties live (regle #9434). Re-exec complete du notebook (70 cellules, 28 code, 0 erreur). Verdict RECOVERABLE-LOCAL -> SOTA-OK."
    - "Socle pedagogique commun : node consistency + AC-3 + Forward Checking + MAC, implementes from-scratch des deux cotes."
    - "Cote C# : extension Choco-solver 4.10.17 via IKVM 8.15.0 (memes recettes infrastructure que CSP-1) pour comparer les algorithmes custom avec la propagation native de Choco (variable x, contrainte arithmetique, model.getSolver().propagate())."
    - "Visualisation : Python = matplotlib (graphes de contraintes colorees par domaine, propagation tracee). C# = sortie textuelle via Console.WriteLine (pas de plotting .NET headless sur ce notebook)."
    - "Idiomes framework : Python = pur (list/dict/set + recursion). C# = generiques System.Collections.Generic + delegation Func<Variable, IEnumerable<int>> pour les domaines."
    - "DRIFT cote Python corrige c.818 (rebaseline fe90bbf6, was 41a4f050) : doc-honesty po-2025 #8139 a substitue des PLACEHOLDER qualitatifs ('petit/moyen/grand', 'reference/moins/le moins', 'modeste/significatif/important') par des nombres reels deterministes (BT 876 assigns / FC 67 / MAC 20 pour N=8) + tableau croisant taille N / variant / ratio BT/MAC. Substance algorithmique inchangee (memes implementations AC-3/FC/MAC). Cote C# inchange (SHA 7c545623 identique)."

date: '2026-08-04'
by: myia-po-2024:CoursIA
python_sha: a7c58274670a247cb90b36ae75c136183170e21b
csharp_sha: f0235302f93669e3b1690dca55506c1ae0f9c485
reason: '#9395 (false-positive on main diagnosis, narrower defect fixed). Issue claimed
  C# twin''s parity claim ''OPTIMAL=7390, identique au km pres avec Python'' was unbacked/false.
  Firsthand re-exec PROVES the claim TRUE: both twins'' CP-SAT solvers return OPTIMAL=7390
  km on the identical schedule (same round-by-round pairings, same per-team travels
  391/1632/1505/1734/1279/849), deterministic 3/3 runs. Root cause of the apparent
  7390-vs-7398 gap: Python compute_schedule_stats summed FLOAT distances (7398) while
  the solver objective truncates int (7390) -- same optimal solution, two rounding
  metrics, NOT a divergence (C# uses long[,] everywhere = self-consistent 7390). Fix:
  (1) App-15 Python c8 now prints solver OPTIMAL+ObjectiveValue(7390), c13 stats made
  int-consistent with the objective (7390 everywhere); (2) App-15b C# c10 now prints
  solver.ObjectiveValue(7390), c15/c16/c39 narrative corrected (the prior ''committed
  Python was FEASIBLE sub-optimal'' explanation was WRONG -- it was a float-display
  metric difference on the OPTIMAL solution). See #8052 D.5, #8364, #4956.'

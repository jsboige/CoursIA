- name: "Search-08 DancingLinks"
  family: Search/Part1-Foundations
  python: MyIA.AI.Notebooks/Search/Part1-Foundations/Search-08-DancingLinks.ipynb
  csharp: MyIA.AI.Notebooks/Search/Part1-Foundations/Search-08-DancingLinks-Csharp.ipynb
  parity_level: semantic
  bridge_verdict: SOTA-OK
  bridge_verdict_reason: |
    Bucket 3 de l'EPIC #10382 (from-scratch = la contrepartie, a ecrire comme telle). Verifie firsthand : Python = stdlib (collections/dataclasses/typing/sys/time) + matplotlib/numpy pour viz polyominos seulement (pas un solveur -- exclu du compte lib per body #10382) ; C# = pur System.* (0 NuGet, 0 Assembly ref). Les DEUX cotes codent DLX from-scratch (Knuth Algorithm X + doubly-linked list node L/R/U/D/C) -- c'est le coeur pedagogique, identique conceptuellement. Il n'existe PAS de lib SOTA-tier pour DLX : l'algorithme = sujet, pas de solveur tiers a brancher (aucun package PyPI ou NuGet n'implemente Dancing Links comme lib appelable -- DLX est par construction un exercice de structure de donnees, la reference est Knuth 2000). Cas bucket 3 : pas d'equivalent .NET (ni Python), le from-scratch EST la contrepartie. L'ancien verdict RECOVERABLE-LOCAL contredisait sa propre raison (qui disait deja "pas de solveur tiers a brancher") -- RECOVERABLE-LOCAL signifie "installable, pas installe", mais il n'y a rien a installer. Flip RECOVERABLE-LOCAL -> SOTA-OK (both from-scratch assume, bucket 3, parite semantique dans l'algo DLX de Knuth).
  known_differences:
    - "Rebaseline c.34 (ai-01) : #8697 a insere le bloc `metadata.cost` (#8056) a l'identique sur les DEUX jumeaux — les deux blob SHA ont bouge, la parite semantique est inchangee. Attestation de parite, pas reparation."
    - "Socle commun : probleme de couverture exacte, algorithme X de Knuth, structure de donnees Dancing Links (DLX), implementation DLX, application polyominos, comparaison de performances DLX vs backtracking."
    - "Asymetrie de couverture : le jumeau Python (60 cellules, 21 code) couvre 3 applications (Sudoku solver, Pavage de polyominos, Pentominoes) + visualisation matplotlib ; le jumeau C# (22 cellules, 10 code) couvre uniquement le pavage de polyominos (Sudoku et Pentominoes absents), pas de matplotlib."
    - "Implementation DLX : les deux cotes codent la structure de listes doublement chainees a la main (noeud avec L/R/U/D/C) -- c'est le coeur pedagogique, identique conceptuellement. Aucun solveur tiers."
    - "Le jumeau C# est explicitement plus compact (tranche pedagogique focalisee sur DLX + polyominos) ; le jumeau Python est la version de reference complete."
    - "2026-08-20 (c.855, myia-po-2025:CoursIA) : flip verdict RECOVERABLE-LOCAL -> SOTA-OK (bucket 3 de #10382, meme classe que Sudoku-07 Norvig #11957). Faux positif : la raison disait deja 'pas de solveur tiers a brancher' mais le verdict restait RECOVERABLE-LOCAL (= installable, pas installe). Verifie firsthand : 0 lib tierce Python (stdlib + matplotlib/numpy viz seulement), 0 NuGet C# (BCL pure), pas de lib SOTA-tier existe pour DLX (la ref = Knuth 2000, DLX est par construction un exercice de structure de donnees). From-scratch des deux cotes = la contrepartie (bucket 3), pas un manque a installer."

- name: "Planners-7 OR-Tools"
  family: SymbolicAI/Planners
  python: MyIA.AI.Notebooks/SymbolicAI/Planners/03-Advanced/Planners-7-OR-Tools.ipynb
  csharp: MyIA.AI.Notebooks/SymbolicAI/Planners/03-Advanced/Planners-7-OR-Tools-Csharp.ipynb
  parity_level: native-both
  bridge_verdict: SOTA-OK
  bridge_verdict_reason: |
    Python = `ortools` (Google, Apache-2.0, PyPI officiel Google, CP-SAT SOTA Job-Shop scheduling :
    `CpModel()` + `NewIntervalVar` + `AddNoOverlap` + `model.Minimize(makespan)` + `solver.Solve()` pour
    l'exemple OR-Tools "a simple example of a job shop problem"). C# = `Google.OrTools` NuGet (Google,
    Apache-2.0, NuGet officiel Google, MEME solveur CP-SAT natif : `#r \"nuget: Google.OrTools\"` +
    `using Google.OrTools.Sat;` + `CpModel()` + `AddBoolVar` + `AddNoOverlap` + `solver.Solve()`).
    Verdict SOTA-OK (modele owner c.217 PR #10557 MERGED 2026-08-12 App tranche 4 lib-vs-lib + PR #10386
    Planners-7 Tranche 2 MERGED substance Google.OrTools validee firsthand exemple OR-Tools "a simple
    example of a job shop problem" : makespan optimal = 11 des deux cotes, memes plans) : les DEUX cotes
    utilisent LE MEME solveur Google OR-Tools CP-SAT natif (meme binding C++ sous-jacent, API symetrique
    entre les bindings PyPI + NuGet officiels Google), bridge direct sans glue non-SOTA. Asymetrie
    documentee : ScottPlot cote C# = helper viz gantt uniquement (le PY utilise matplotlib heatmap
    equivalent, asymetrie mineure de lib viz n'impactant pas le semantisme). `parity_level: native-both`
    preserve : parite engine.
  known_differences:
    - "Concept : planification orientee satisfaction de contraintes (Job-Shop scheduling, CP-SAT)."
    - "Python : OR-Tools CP-SAT Python bindings (solveur SOTA). C# : deux tranches — Tranche 1 from-scratch BCL .NET (0 NuGet, backtracking + propagation, enseigne les mecanismes) + Tranche 2 Google.OrTools CP-SAT natif .NET (moteur de production, depuis 2026-08-11)."
    - "Parite engine (native-both depuis 2026-08-11) : les deux cotes atteignent un moteur CP-SAT de production (Python OR-Tools, C# Google.OrTools). Le from-scratch C# reste EN PLUS (jamais a la place), critere owner #10382. Meme exemple de reference : exemple OR-Tools \"a simple example of a job shop problem\" (3 jobs / 3 machines, 8 operations, makespan OPTIMAL = 11 des deux cotes)."

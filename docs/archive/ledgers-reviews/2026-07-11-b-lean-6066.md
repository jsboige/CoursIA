> **ARCHIVED 2026-07-19** — Verdict **SWEEP DATÉ PM TRANSIENTE**. Sweeps datés PM transiente (juillet 2026, audit H.4). Document conservé pour référence historique (daté, immutable). Voir triage table c.730/PR [#7450](https://github.com/jsboige/CoursIA/pull/7450) + dossier [`docs/archive/ledgers-reviews/INDEX.md`](INDEX.md). Superseded by mini-Epic #7422 PR #2/3 (hygiène docs/ avant publication #4208 + traduction #1650). *Archivé par : `po-2025` (lane `myia-po-2025:CoursIA-2`, c.736, [See #7422](https://github.com/jsboige/CoursIA/issues/7422)).*

# §B Lean Forensic — PR #6066 (hashlife N2 step 3 reach-within-padCenter2-margin capstone)

- **PR**: #6066 — `feat(hashlife): N2 step 3 reach-within-padCenter2-margin capstone (c.311)` (See #3846)
- **Author**: po-2026 (lane `myia-po-2026:CoursIA`, c.311)
- **Reviewer**: po-2023 (lane `myia-po-2023:CoursIA`) — cross-lane
- **Date**: 2026-07-11
- **File**: `MyIA.AI.Notebooks/SymbolicAI/Lean/conway_lean/Conway/Life/LightCone.lean` (+50/−0)
- **Base**: `origin/main` (fresh, two-dot diff `origin/main..origin/feature/conway-n2-step3-margin`)
- **Verdict**: **MERGEABLE (§B PASS, sorry-free capstone)**

## Scope

EPIC #3846 (conway_lean Hashlife), N2 step 3. Adds a single sorry-free Grid-level theorem `evolve_reach_within_padCenter2_margin` to `LightCone.lean`: after `2^k` generations of evolution, every alive cell `q` has each coordinate within the `padCenter2` per-side margin `3·2^(k-1)` of some initially-alive cell `p`. This is the **set-distance half** of the N2 step 3 bridge ai-01 greenlit (the arithmetic half landed in #5975/c.309). The MacroCell offset-wire into `p5_large_n_jump` remains P4-gated, out of scope.

Distinct from #6045 (c.125, Utility.lean i18n docstring translation): this is a **theorem proof** (substance), not a docstring translation. Same §B register, distinct sub-register.

## §B Lean forensic (independent, two-dot byte-identity)

| # | Criterion | Method | Result |
|---|-----------|--------|--------|
| F1 | sorry real-tactic delta (doc comments stripped) | `re.sub('/-.*?-/','')` + `re.sub('--[^\n]*','')` + `\bsorry\b`, MAIN vs BRANCH, both files | LightCone 0→0 (new lemma sorry-free) ; HashlifeCorrectness 2→2 (untouched). **Real sorry unchanged = 2** (L2734 P4 offset-assembly, L3063 `p5_large_n_jump`). ✓ |
| F2 | imports byte-identical | two-dot diff scan for `^import`/`open`/`namespace` | +50/−0, **0 import/namespace changes** ✓ |
| F3 | CRLF = 0 | `git diff \| grep -c '\r'` | **0 CRLF lines** ✓ |
| F4 | declaration-keyword delta | `\btheorem\b` / `\blemma\b` count MAIN vs BRANCH | theorems 16→17 (+1), lemmas 0→0. **Pure additive, 0 removals** ✓ |
| F5 | Lean CI PASS = `lake build` SUCCESS proxy | `gh pr checks 6066` | **`ci / Lean CI (conway_lean)` PASS 4m18s**. Body also cites local `lake.exe` v4.31.0-rc1 SUCCESS 2985 jobs 0 errors. ✓ |
| F6 | convention coherence (proof sound by construction) | read theorem tactic | `obtain` (reach) → `have` (per-coord bounds + margin) → `exact ⟨p, hp, hb1.trans hmargin, hb2.trans hmargin⟩`. **`Nat.le.trans` composition: `|q.i−p.i| ≤ 2^k` ∧ `2^k ≤ 3·2^(k-1)` ⟹ `|q.i−p.i| ≤ 3·2^(k-1)`. Mathematically airtight (D5 monotonicity by-construction).** ✓ |

## Dependency resolution (3 deps, all exist on MAIN, G.1 firsthand)

The theorem composes three already-sorry-free ingredients — independent grep confirms each resolves:

| Dependency | Location (MAIN) | Role |
|------------|-----------------|------|
| `evolve_reach_chebyshev` | LightCone.lean (N2 step 2) | tight Chebyshev reach `chebDist p q ≤ 2^k` |
| `coord_bound_of_chebDist_le` | LightCone.lean | per-coord `|q.i − p.i| ≤ 2^k` |
| `padCenter2_margin_ge_jumpReach` | **HashlifeCorrectness.lean** (#5975/c.309) | margin arithmetic `2^k ≤ 3·2^(k-1)` |

Lean CI PASS confirms all three resolve with the expected signatures (else `lake build` would fail on the `obtain`/`have` applications).

## G.1 cross-check of body claims (verify, don't propagate)

| Body claim | Independent verification | Verdict |
|------------|--------------------------|---------|
| "real sorry unchanged = 2" | doc-stripped count: MAIN 2 / BRANCH 2 (L2734 + L3063) | **MATCHES** ✓ |
| "3 deps sorry-free" | all 3 exist on MAIN; new theorem sorry-free | **MATCHES** ✓ |
| "+50/−0, 1 file" | two-dot `--stat` | **MATCHES** ✓ |
| "Lake build SUCCESS" | Lean CI (conway_lean) PASS 4m18s | **MATCHES** ✓ |
| "Correction to ai-01 DM (3→2 via #5998 merged 06:49Z)" | my count = 2, consistent with post-#5998 state | **CORROBORATED** ✓ (body honest, flags DM staleness) |

Note on naive `grep -c sorry`: returns **40** on HashlifeCorrectness (prose mentions in doc comments: "Every theorem is stated precisely with `sorry`", "Each `sorry` is a self-contained prover target", …). **Only doc-stripped count (2) is meaningful** — naive grep would over-count 20×. c.85 lesson applied (`/-…-/` block-comment sorry = prose, not tactic).

## Anti-regression §D

- Pure addition (+50/−0), **0 deletions** on production Lean code.
- The 2 residual sorries of `HashlifeCorrectness.lean` (P4-gated research) are **untouched** (file not in diff).
- No `sorry`/`sorryAx`/`admit` added anywhere.

## Bot-review guidance

Verdict `MERGEABLE (§B PASS, sorry-free capstone)`. Lean substance: a sorry-free theorem proving the set-distance half of the N2 step 3 bridge, composed from 3 verified-sorry-free deps via `Nat.le.trans`. All §B forensics PASS firsthand (F1-F6), G.1 cross-check of every body claim MATCHES, anti-regression clean (pure additive). Lean CI PASS = lake build SUCCESS. Body is scientifically honest (corrects ai-01's stale DM sorry count 3→2). `See #3846` correct (P4-gated MacroCell wire remains). Safe to merge.

## Rules referenced

- `../../../.claude/rules/lean-merge-discipline.md` (F5: Lean CI PASS = lake build SUCCESS proxy)
- `../../../.claude/rules/anti-regression.md` (F1/F4: sorry delta + pure additive, 0 deletions on production proof code)
- `../../../.claude/rules/verify-before-claiming.md` (G.1: verify claims AND verdicts against source firsthand)

# §H.4 + SOTA forensic — PR #6112 (Z3-Python-07 declarative LINQ ↔ pyz3, See #1206)

**Auditeur** : `myia-po-2024:CoursIA` (cross-lane, po-2023 substance)
**Date** : 2026-07-11
**Cible** : PR #6112 `Add: Z3-Python-07 du style déclaratif LINQ au solveur Z3` (po-2023, +536/-1, 2 fichiers)
**Base** : `origin/feature/z3py07-declaratif-linq` vs `origin/main` (two-dot)
**Verdict** : **MERGEABLE — SOTA-OK, EXEC_PROVED**

## Contexte

PR #6112 = livrable de la tranche #1206 (Fork Z3.Linq + série notebooks) greenlit par ai-01
(msg steer po-2023). Ajoute `Z3-Python-07-Style-Declaratif-Linq.ipynb` (nouveau notebook Python)
+ met à jour le README de la série Z3-Python (compte 6→7). Pont explicite entre l'idiome
déclaratif LINQ C# (Z3.Linq) et l'API impérative pyz3.

## Forensic D1–D7 (firsthand, contre le blob de la branche)

### D1 — Structure EXEC_PROVED (règle H.1 / C.2)
- 19 cellules (7 code + 12 markdown).
- **7/7 cellules code `execution_count` != None** (1–7).
- **0 cellule avec output `error`**.
- Verdict H.5 : **EXEC_PROVED** (outputs réels, pas `STRUCTURAL_ONLY`).

### D2 — C.1 : aucune erreur volontaire (règle C.1)
- Scan whole-notebook : aucun `raise NotImplementedError`, `assert False`, `1/0`,
  `except: pass`, `except Exception: pass`.
- Les 3 cellules d'exercice (cell 13/15/17) sont des **scaffolds en commentaires purs**
  (`# TODO etudiant : ...`) initialisés avec `s_exN = Solver()` → s'exécutent en no-op
  genuine, `execution_count` renseigné, message « a completer » en sortie stream.
- Conforme C.1 (le notebook s'exécute end-to-end, exercices non complétés inclus).

### D3 — SOTA Prong A : vrai outil z3-solver, pas workaround (règle sota-not-workaround §A)
- **Import réel** : `from z3 import *` (cell 1), version rapportée `z3-solver v4.15.4`
  (cellule de vérification d'environnement, output stream « Imports OK »).
- **Vraies invocations Solver()** :
  - cell 4 : théorème SAT à 5 entiers / 4 contraintes → `Resultat du solveur : sat`.
  - cell 7 : système UNSAT (`x > 5 ET x < 3`) → extraction du **noyau d'insatisfiabilité**
    via `assert_and_track` / `unsat_core`.
  - cell 10 : **coloration 3-couleurs de la carte d'Australie** → modèle trouvé.
- Aucune réimplémentation jouet, aucun stub d'appel de service, aucune sortie fabriquée.
- Verdict Prong A : **SOTA-OK** (le vrai outil z3 est proprement invoqué ; les sorties
  committee sont ses vraies sorties `sat`/`unsat`/modèle).

### D4 — SOTA Prong B : problème non-trivial (règle sota-not-workaround §B)
- Le notebook ne démontre PAS z3 sur un cas dégénéré où le SOTA équivaut à une baseline.
- **Coloration de graphe (carte d'Australie, 3 couleurs)** = CSP combinatoire canonique
  non-trivial qui exerce réellement la capacité distinctive du solveur (propagation +
  backtrack sur un graphe 6-nœuds 7-arêtes). L'Australie est 3-coloriable mais PAS
  2-coloriable (contient un triangle WA–NT–SA) — l'Exercice 3 exploite précisément
  cette asymétrie (2-coloriable ? → UNSAT).
- L'extraction d'`unsat_core` (cell 7) est une capacité SOTA distincte (au-delà d'un
  simple `check()` booléen) qui justifie le solveur.
- Verdict Prong B : **OK** (problème discriminant mais borné, runtime notebook raisonnable).

### D5 — Convention #2161 : ≥3 exercices (règle three-exercises-per-notebook)
- **3 en-têtes `### Exercice`** (cell 12 md : Exercices 1/2/3) + **3 cellules code stub**
  (cell 13/15/17), une par exercice.
- Stubs répartis (pas tous en fin) : Exercice 1 après section 3, Exercice 2 après section 4,
  Exercice 3 après section 4 — suit la convention « répartis dans le notebook ».
- Chaque stub est précédé d'un markdown d'objectif + indices. Conforme #2161.

### D6 — Pont LINQ ↔ pyz3 (but #1206)
- Section 1 (cell 2) : présente l'idiome déclaratif LINQ C# Z3.Linq comme **référence**
  (le `where`/`select` déclaratif).
- Section « correspondance LINQ <-> pyz3 » (cell 5) : traduit explicitement l'intention
  déclarative en appels impératifs `s.add(...)`.
- Le but #1206 (pont C# Z3.Linq déclaratif ↔ pyz3 impératif) est **atteint** et rendu
  visible dans la structure pédagogique.

### D7 — README fidèle + CATALOG intact
- README Z3-Python : intro « 6 notebooks → **7 notebooks** » + ligne de table `07` ajoutée
  (description `assert_and_track`, `unsat_core`, coloration Australie — **fidèle** au contenu
  réel du notebook, pas de fabrication) + ligne « Fil pédagogique » 7 ajoutée.
- **Marqueur `CATALOG-STATUS` non touché** (byte-identical à main — diff ne touche que
  prose + table notebooks).
- CRLF = 0 sur le blob notebook (LF-only préservé).

## G.1 cross-check
- Tout claim du body (pont LINQ↔pyz3, unsat_core, coloration graphe, 3 exercices) MATCH
  le contenu réel du notebook firsthand.
- Aucun output fabriqué (les sorties `sat`/`unsat`/modèle sont cohérentes avec le
  comportement réel de z3 sur ces instances : 5-entiers satisfiable, x>5∧x<3 insatisfiable,
  Australie 3-coloriable, Australie 2-coloriable = UNSAT).
- Anti-régression : notebook nouveau (additif), 0 suppression de contenu existant.

## Observations non-bloquantes
- Aucune. Notebook de qualité PRODUCTION, conforme C.1/C.2, SOTA-OK Prong A+B, #2161.

## Recommandation
**MERGE.** Substance réelle (nouveau notebook z3, pont #1206), exécution prouvée
(EXEC_PROVED), SOTA-OK (vrai solveur, problème non-trivial), conventions respectées
(C.1, C.2, #2161, README fidèle). Aucun blocage identifié.

See #1206 (partial — 1 notebook Z3-Python-07, l'EPIC Z3.Linq reste multi-tranches).

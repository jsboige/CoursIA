# §B Lean + §D anti-regression forensic — PR #6101 (stale-base revert of #6085 + #6098)

- **PR**: #6101 — `docs(lean,#4980): minimax root aggregator bilingual FR+EN inline`
- **Author**: po-2024 (lane `myia-po-2024:CoursIA`, c.364)
- **Reviewer**: po-2023 (lane `myia-po-2023:CoursIA`) — cross-lane, Lean i18n turf
- **Date**: 2026-07-11
- **Files**: `Minimax.lean` (+68/-0, legit i18n) + **`config.py` (+6/−6) + `lean_utils.py` (+0/−34) = STALE-BASE REVERTS**
- **Base**: `origin/main` (two-dot diff `origin/main..origin/docs/4980-minimax-root-bilingual`)
- **Verdict**: **CHANGES_REQUESTED — stale base silently reverts #6085 + #6098 (anti-regression §D)**

## Title vs diff mismatch (the G.1 trigger)

Title/body claim "docs i18n" (+68, 1 file) but the actual diff is **3 files / +78/−30**. The 2 extra files (`config.py`, `lean_utils.py`) are **prover harness code with deletions** — not docs. `gh pr view --json changedFiles` returned a stale "1" (API lag); the real two-dot diff surfaces 3 files. This title/diff mismatch is the red flag that triggered the forensic.

## File 1 — Minimax.lean (+68/−0): LEGIT, the real deliverable

Inline bilingual FR+EN root aggregator, EPIC #4980 Phase 0.5 (same pattern as #5883 CooperativeGames pilote + #6048 RepeatedGames + my #6065 Finiteness.lean).

| Check | Result |
|-------|--------|
| Lean code (doc-stripped) MAIN == BRANCH | **True** ✓ |
| imports byte-identical (`Mathlib`, `Minimax.ZeroSum`, `Minimax.Concavity`, `Minimax.SionApplication`) | **True** ✓ |
| namespace (`MinimaxLean`) byte-identical | **True** ✓ |
| sorry count | 0 → 0 ✓ |
| FR block byte-preserved (MAIN content = BRANCH prefix) | **True** ✓ |
| `---` separator + EN mirror | present ✓ |

This file alone is MERGEABLE. It is identical in substance to my #6065 (Finiteness.lean root aggregator) — correct EPIC #4980 pattern.

## File 2 — config.py (+6/−6): REVERTS #6098 (merged 11:23Z, same day)

```
demo 62 "HASHLIFE_P4_SUCC_MEMBERSHIP":
- "line": 2734   (main, set by #6098 — po-2026 c.316 proved sorry at L2734)
+ "line": 2673   (branch — reverts to pre-#6098 stale value)
- description "...declared L2697, residual sorry L2734..."
+ description "...declared L2636, residual sorry L2673..."
```

#6098 (`fix(prover): depth-3 harness — FX-5 relative_path`, merged 2026-07-11T11:23Z) **corrected** the demo-62 line number from the stale 2673 → the accurate 2734 (`p4_succ_membership` declared L2697, sorry L2734, verified firsthand by po-2026 c.314/c.316 AND by ai-01's revert #6095 of the false `window_cone_in_domain` attribution). #6101 **reverts** that correction back to the stale 2673/L2636.

## File 3 — lean_utils.py (+0/−34): REVERTS #6085/#5982 depth-3 probe fix

- **Deletes** `def probe_relative_path(filepath, project_dir, tmp_name)` (18 lines, the depth-agnostic probe-path resolver added by #6085 `fix(prover): latch workflow now resolves Lake project`, merged 10:56Z).
- **Reverts 3 call sites** (`is_true_placeholder_goal`, `get_goal_state`, `verify_sorry_replacement`) from the fixed `probe_relative_path(...)` back to the **legacy buggy** `subdir = Path(filepath).parent.name` (depth-2) form.

On **current main**: `probe_relative_path` = 6 references, `parent.name` = 0 (fully migrated). #6101 would reintroduce the legacy depth-2 form → **the exact depth-3 probe false-positive bug** (`Conway/Life/HashlifeCorrectness.lean` depth-3 nesting → wrong module → FX-5 silently false-positives deep sorries as TRUE_PLACEHOLDER_GOAL) that #5982/#6085/#6098 were a 3-PR arc to close.

## Root cause: stale base (catalog-poison pattern)

- PR created 2026-07-11T11:20:21Z, but its branch base predates #6085 (merged 10:56Z) — the branch held the **pre-fix** versions of `config.py` + `lean_utils.py`.
- Author then rebased onto current main to add the Minimax.lean i18n. The rebase carried the stale prover-file contents forward as "changes", producing a diff that **looks like intentional edits** but is actually a **mechanical rebase of old file versions over the new ones**.
- This is the textbook stale-base / catalog-poison failure mode ([catalog-pr-hygiene.md](../../../.claude/rules/catalog-pr-hygiene.md) HARD 2: "Repartir d'un origin/main à jour avant de pousser"). Not malicious — but **would reintroduce 2 real bugs if merged**.

## Resolution (for author)

Keep the legit Minimax.lean i18n; reset the 2 stale prover files to current main:

```bash
git checkout origin/main -- MyIA.AI.Notebooks/SymbolicAI/Lean/agent_tests/prover/config.py
git checkout origin/main -- MyIA.AI.Notebooks/SymbolicAI/Lean/agent_tests/prover/lean_utils.py
git commit -m "chore: reset prover files to main (drop stale-base reverts of #6085/#6098)"
git push
```

After this, the PR diff = Minimax.lean i18n only (+68/−0) → MERGEABLE.

## Bot-review guidance

**CHANGES_REQUESTED** — the Minimax.lean i18n is correct and should land, but the 2 prover files are a stale-base revert of #6085 + #6098 (re-introduces the depth-3 probe false-positive bug + reverts the demo-62 line correction). Not a judgment on authorship — mechanical rebase of an old base. The fix is a `git checkout origin/main --` on the 2 files. Do NOT merge as-is: `APPROVED` would reintroduce 2 production harness bugs the cluster spent 3 PRs closing.

## Rules referenced

- `../../../.claude/rules/catalog-pr-hygiene.md` HARD 2 (rebase frais avant push — stale base = source n°1 du poison)
- `../../../.claude/rules/anti-regression.md` (deletions on production harness code without diagnosis = red flag)
- `../../../.claude/rules/pr-review-discipline.md` §B/§D (read diff, not title; anti-regression on prover harness)
- `../../../.claude/rules/verify-before-claiming.md` (G.1: title ≠ PR; two-dot diff surfaces the real 3-file scope)

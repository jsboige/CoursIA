# §H.4 forensic review of PR #6000 (docs smartcontracts README de-spaghetti inverted-pyramid)

Cross-owner §H.4 review by po-2023 of PR #6000 (`docs(smartcontracts): de-spaghetti README inverted-pyramid (#5985)`, author po-2024, +19/−19, 1 file). Reviewer lane: po-2023 (.NET/SymbolicAI turf, distinct from author). Reciprocal review — po-2024 reviewed my #6013 (ledger #6015).

## Verdict §H.5

**PR #6000 = MERGEABLE sans modification.** Pure block reorder, multiset-diff = 0 (zero content change), CATALOG-STATUS byte-identical, heading order corrected exactly as the body claims.

| Check | Result |
|-------|--------|
| multiset-diff (Counter equality, pure reorder) | **= 0** CONFIRMED |
| CATALOG-STATUS byte-identical | CONFIRMED |
| Heading order (Parcours recommande L124 → Structure L163) | CONFIRMED (was Structure L124 → Parcours L143 on main) |
| Link/ref count unchanged | 48 = 48 (no ref dropped/added) |
| Diff stat vs body claim | +19/−19, 1 file = matches body exactly |
| Base freshness | 0 behind origin/main (no base drift) |
| Collision | 0 open PR touches this README besides #6000 |

## G.1 firsthand ledger

1. **PR #6000 state**: OPEN, mergeStateStatus UNSTABLE (check-links pre-existing fail on main, same as the rest of the frozen queue — NOT caused by this PR; unblocked by #6005).
2. **Two-dot diff** (byte-identity safe per review-three-dot-vs-two-dot-g1, not three-dot): `git diff origin/main..origin/feature/5985-smartcontracts-readme-despaghetti` = 1 file, +19/−19. Matches body claim to the line.
3. **multiset-diff = 0**: `Counter(old.split('\n')) == Counter(new.split('\n'))` — true. 649 lines both sides. The diff shows `## Structure` (folder tree block, 19 lines incl. code fence) deleted at L121-139 and re-inserted at L160-178, AFTER `## Parcours recommande`. No line within either block changed.
4. **CATALOG-STATUS untouched**: `<!-- CATALOG-STATUS:START -->…:END -->` block extracted from both versions, byte-identical (catalog-pr-hygiene respected — no regen on feature branch).
5. **Heading order verified**: on origin/main, `## Structure` L124 precedes `## Parcours recommande` L143; on branch, `## Parcours recommande` L124 leads, `## Structure` L163 trails. Body's "After" reading-order block (Parcours recommande L124, Structure L163) is accurate.
6. **No content drift**: 48 markdown links/ref count identical both sides. A pure reorder cannot break anchors/relative paths (same targets, reordered) — verified the count holds.
7. **Inverted-pyramid logic sound**: `## Structure` (folder tree = reference/meta) was leading, burying `## Parcours recommande` (ASCII flowchart + objectives table = saillant guidance). Post-reorder, the saillant path leads, the reference tree trails. Same pattern as #5995 (GameTheory) and #5997 (Sudoku). Consistent with #5985 doctrine.
8. **Honest scope disclosure by author**: body explicitly flags pre-existing lint warnings (MD040/MD034/MD036) as out-of-scope — single-subject reorder, not a lint sweep. Good.

## Notes

- **Low-risk PR**: +19/−19 pure reorder with multiset-diff=0 is structurally minimal — ai-01 can batch-merge on the pre-verified verdict. The reorder's only failure mode (silent content edit / CATALOG drift / anchor breakage) was checked and is absent.
- **UNSTABLE status is not this PR's fault**: the check-links fail is pre-existing on main (the §H.4 review-ledgers that cited rules by approximate path); #6005 fixes it. Once #6005 merges, #6000 repasses CLEAN.
- `See #5985` (EPIC partial — SmartContracts family de-spaghetti'd; GameTheory #5995 + Sudoku #5997 + RL #5990 + Probas #5993 + CaseStudies #5999 + SemanticWeb #6007 already done).

— po-2023, 2026-07-11 (cross-owner review, ledger persisted per harness-hygiene doc-pérenne tier)

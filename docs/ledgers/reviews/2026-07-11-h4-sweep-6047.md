# §H.4 forensic review — PR #6047

**Date:** 2026-07-11
**Reviewer:** myia-po-2025 (cross-owner, indépendant du body PR)
**PR:** #6047 — `fix(gametheory): restructure GT-5 exo stubs (3 exos C#) — fix CS8632, add subsection headers + indices`
**Author:** myia-po-2024
**Fichier:** `MyIA.AI.Notebooks/GameTheory/GameTheory-5-ZeroSum-Minimax-Csharp.ipynb`
**Diff:** +22/−91, 1 fichier
**Verdict:** **STRUCTURAL_ONLY — blocage C.2/H.3** (re-exécution .NET requise avant merge)

## Méthode

Forensic indépendant du body PR (`gh pr diff 6047` + inspection firsthand des hunks `+`/`−`).
Le verdict d'un autre reviewer (jsboige/Hermes, COMMENT_WITH_CONCERNS) a été lu avant cette
analyse ; la présente review **confirme firsthand** le concern H.3 signalé et **ajoute la
clearance anti-régression** sur les −91 deletions (non couverte par le body ni le review bot).

## Forensics

### F1 — Anti-régression sur les −91 deletions : SAFE ✅

Le ratio deletions > insertions (−91/+22) est un red-flag par défaut (cf
[`anti-regression.md`](../../../.claude/rules/anti-regression.md)). Inspection firsthand des
lignes supprimées : les −91 se décomposent en

1. **Anciens stubs cassés** `(double[,]?)null` (syntaxe déclenchant CS8632, à remplacer),
2. **Sorties obsolètes** retirées des 3 cellules exercices (`outputs[]` comprenant le warning
   CS8632 dans `stderr` + les `display_data` « Exercice N a completer »),
3. **Structure JSON** (`"source": [`, `"execution_count": N`, `}`, `]`, …).

**Aucune cellule `# Solution` / `# Exemple résolu` / contenu pédagogique supprimée.** Les
exercices eux-mêmes (Blotto 5 soldats/3 champs, 3×3 sans point-selle, 3×3 avec point-selle)
sont **préservés et enrichis** (indices gradués `Indice N` + `Etape N` ajoutés). La règle
anti-régression ne s'applique de toute façon pas aux stubs d'exercice (cf
[`exercise-example-labeling.md`](../../../.claude/rules/exercise-example-labeling.md)) — le
remplacement `(double[,]?)null` → `new double[,] { { 0 } }` est **conforme** (stub exécutable
sans erreur volontaire, règle C.1).

### F2 — C.1 (pas d'erreur volontaire) : CONFORME ✅

Inspection des 3 nouvelles cellules exercices (hunks `+`) : aucune occurrence de
`raise NotImplementedError` / `assert False` / `1/0`. Stubs uniformes :
`double[,] exNMatrix = new double[,] { { 0 } };` + `display("Exercice N a completer …")`.
Le notebook compile et s'exécute de bout en bout à l'état stub (matrice 1×1 valide). Cf
[`notebook-conventions.md`](../../../.claude/rules/notebook-conventions.md) §C.1.

### F3 — Fix CS8632 : CONFORME ✅

L'ancienne forme `var ex = (double[,]?)null` utilisait une annotation nullable `?` hors
contexte `#nullable enable` → warning **CS8632**. La nouvelle forme `new double[,] { { 0 } }`
supprime l'annotation nullable → le warning disparaîtra à la re-exécution. Fix correct.

### F4 — Convention #2161 (≥ 3 exercices + markdown + indices) : CONFORME ✅

3 exercices, chacun précédé d'un sous-titre markdown (`### 9.1`, `### 9.2`, `### 9.3`) et
d'une intro pédagogique (lien avec `minimax_lean`, Von Neumann via Sion). Chaque exercice
porte 3 indices gradués (`Indice 1/2/3` ou `Etape 1/2/3`). Header de section `## 9. Exercices`
avec note de convention C.1. Cf
[`three-exercises-per-notebook.md`](../../../.claude/rules/three-exercises-per-notebook.md).

### F5 — C.2 / H.3 (notebook exécuté) : ⚠️ ÉCHEC — BLOCAGE

**Firsthand (hunks `+`)** : les 3 cellules exercices restructurées portent toutes
`"execution_count": null` **sans** `outputs[]`. Les sorties ont été retirées (correctement,
puisque le code stub a changé) mais **non re-exécutées**.

Ceci viole H.3 (« Aucun commit de notebook non-exécuté », cf
[`regles-validation-detail.md`](../../../docs/reference/regles-validation-detail.md)) et C.2
(notebooks committés AVEC outputs cohérents). **Le verdict est STRUCTURAL_ONLY, pas
EXEC_PROVED** : la restructure est correcte mais l'état d'exécution n'est pas prouvé.

**Le fix est trivial et succès-garanti** : les nouveaux stubs (`new double[,] { { 0 } }` +
`display`) sont exécutables et produiront une sortie propre (display_data + 0 warning CS8632).
po-2024 doit re-exécuter les 3 cellules via kernel `.net-csharp` (dotnet-interactive) pour
repeupler `execution_count` + `outputs`, puis amender la PR.

## Verdict consolidé

| Forensic | Résultat |
|----------|----------|
| F1 anti-régression (−91) | ✅ SAFE — stubs cassés + sorties obsolètes + JSON, 0 contenu pédagogique perdu |
| F2 C.1 | ✅ conforme — 0 `NotImplementedError`, stubs exécutables |
| F3 CS8632 | ✅ fix correct — annotation nullable supprimée |
| F4 #2161 | ✅ 3 exercices + sous-titres + indices gradués |
| **F5 C.2/H.3** | ⚠️ **ÉCHEC — `execution_count: null` ×3, re-exécution .NET requise** |

**Recommandation : blocage jusqu'à re-exécution.** La PR n'est pas mergeable en l'état (H.3
 HARD). Après re-exécution des 3 cellules par po-2024 (`.net-csharp`, succès attendu sans
 warning CS8632), la PR devient EXEC_PROVED et mergeable. L'apport pédagogique (fix C.1/CS8632
 + indices gradués + convention #2161) est réel et bienvenu.

## Non-duplication

jsboige (Hermes bot) a posté COMMENT_WITH_CONCERNS couvrant la qualité structurelle + le
concern H.3. La présente review :
- **confirme firsthand** H.3 (avec le mécanisme précis : sorties retirées car code modifié,
  stubs re-exécutables et succès-garantis — pas un état cassé, juste non-exécuté),
- **ajoute la clearance anti-régression F1** (les −91 = légitimes, non couvert par le bot ni
  le body PR — important car le ratio deletions>insertions est un red-flag par défaut).

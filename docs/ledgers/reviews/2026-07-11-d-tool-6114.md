# §D tool forensic — PR #6114 (check_docs_links skip git submodule internals)

**Auditeur** : `myia-po-2024:CoursIA` (cross-lane, po-2023 harness fix)
**Date** : 2026-07-11
**Cible** : PR #6114 `fix(harness): skip git submodule internals in check_docs_links` (po-2023, +40/-4, 1 fichier `scripts/check_docs_links.py`)
**Base** : `origin/fix/check-links-skip-submodules` vs `origin/main` (two-dot)
**Verdict** : **MERGEABLE — strict defensive improvement, 0 régression** (avec **observation NON-BLOQUANTE majeure** : la prémisse du body ne reproduit pas firsthand)

## Le fix (what it does)

1. `_load_submodule_paths()` : lit les chemins de sous-modules depuis `.gitmodules` (forme posix, robuste à l'absence/fichier non-parsable → retourne `set()` vide).
2. `_should_skip()` : saute désormais tout chemin sous un sous-module (en plus de `SKIP_DIRS`).
3. `check_link()` : traite les cibles dans un sous-module comme valides (return True) — robuste au cas CI `submodules: false` où le mount dir est absent.

## Forensic D1–D4 (firsthand)

### D1 — Logique sound
- `_load_submodule_paths()` : parsing `.gitmodules` correct (lignes `path = ...`, `partition("=")`, strip). Guard `is_file()` + `try/except OSError` → robuste. ✓
- Préfixe-match `rel_posix == sm or rel_posix.startswith(sm + "/")` : pas de false-positive (`Z3` ne matche pas `Z3.Linq`). ✓
- `SUBMODULE_PATHS` computed at module-load (constant). ✓

### D2 — Exécution firsthand
- Sur la branche : `Scanned 562 files, 3780 links — No broken links found. exit=0`. ✓
- `py_compile` clean (pas d'erreur de syntaxe).

### D3 — Solve le problème annoncé ? **PRÉMISSE NON REPRODUITE firsthand** (observation majeure non-bloquante)
Le body annonce « 4 liens relatifs cassés dans les internals des sous-modules (Argumentum, MetaGeneticSharp) → rougit la CI check-links ». **Vérification firsthand : la prémisse ne tient pas** :
- **main = 0 broken link** dans DEUX environnements : (a) worktree fraîche sans sous-modules checkés, (b) arbre partagé avec **les 5 sous-modules présents** (forge-std 3 .md, openzeppelin 41 .md, account-abstraction 1 .md, Z3.Linq, MetaGeneticSharp).
- **Z3.Linq/README.md** (le sous-module avec liens relatifs) : les 3 liens testés (`examples/z3-problems.dib`, `solutions/Z3.Linq.Demo/Program.cs`, `docs/blogs/part-01-...md`) **existent et résolvent correctement** (relatifs au dir du fichier source, pas au repo root).
- **Aucun doc CoursIA ne link VERS une cible de sous-module** (grep `Z3.Linq/|MetaGeneticSharp/|foundry-lib/lib/` dans les .md = 0 hit) → le changement `check_link` (cible submodule valide) n'a **aucun trigger actuel**.
- **CI workflow** : pas de directive `submodules:` → défaut GitHub = `false` → les internals des sous-modules **ne sont jamais scannés en CI**.
- **Argumentum** (cité dans le body) n'est **pas un sous-module de ce dépôt** (référence cross-workspace).

**Conclusion D3** : la CI red d'aujourd'hui était le **lien pendant du ledger #6086** (`c427-count-exercises-bug1-2161.md` jamais créé), **fixé par #6104 (MERGED)** — PAS les liens de sous-modules. Le body de #6114 **conflitgue les deux causes** ou surdéclare la prémisse.

### D4 — Minimal, 0 régression
+40/-4, 1 fichier, pure additif (nouvelle fn + 2 gardes). Le ancien `parts` → `rel` pour SKIP_DIRS est équivalent. `py_compile` clean. ✓

## Verdict

**MERGEABLE** — c'est une **amélioration défensive stricte** (0 régression, code clean, durcit le harnais contre : futurs sous-modules à structure interne incomplète, futurs docs CoursIA linkant vers des sous-modules sous CI `submodules:false`). Le merge ne fait pas de mal.

**MAIS** : le body devrait être **amendé** pour refléter la vraie valeur (défensif/future-proofing) plutôt que « fixe 4 broken links / active CI red » (prémisse non reproduite firsthand). Ce n'est pas bloquant (le code est un strict improvement), mais ai-01 / l'auteur devraient savoir que la justification originale est inexacte — la CI red résiduelle était #6086 (fixé #6104), pas les sous-modules.

## Recommandation
**MERGE** (strict improvement). Optionnel : amend body pour framing défensif, ou follow-up doc. Pas de CHANGES_REQUESTED — aucun critère §A-§H violé (le fix est correct, minimal, non-régressif ; la prémisse inexacte est un enjeu de documentation du body, pas du code).

See EPIC #3801 (harness robustness) + mandate harness-fix.

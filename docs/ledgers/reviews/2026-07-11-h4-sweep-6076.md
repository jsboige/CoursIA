# §H.4 Notebook Forensic — PR #6076 (GT-7 ExtensiveForm exo stubs restructure)

- **PR**: #6076 — `Add: gametheory GT-7 exo stubs (3 subsections, 9 indices, 3 stubs unified)` (See #2161)
- **Author**: po-2024 (lane `myia-po-2024:CoursIA`, c.414 GameTheory owner-lane)
- **Reviewer**: po-2023 (lane `myia-po-2023:CoursIA`) — cross-owner
- **Date**: 2026-07-11
- **File**: `MyIA.AI.Notebooks/GameTheory/GameTheory-7-ExtensiveForm.ipynb` (+236/−238)
- **Base**: `origin/main` (fresh, two-dot diff `origin/main..origin/feature/gt7-exo-enrich-c414`)
- **Verdict**: **MERGEABLE (EXEC_PROVED)**

## Scope

GT-7 (extensive-form games) was below the #2161 threshold (≥3 exercises/notebook). Section 8 was fragmented: 3 textual exercises + 2 standalone exercises + 2 dispersed code stubs. This PR reorganizes section 8 into a unified pedagogical structure: 3 markdown subsections (`### 8.1/8.2/8.3`) + 9 graduated hints (3/exo) + 3 unified code stubs. The −238 deletions = standalone fragment cells consolidated INTO the unified structure (content preserved, not lost).

## §H.4 notebook forensic (independent, two-dot byte-identity)

| # | Criterion | Method | Result |
|---|-----------|--------|--------|
| F1 | Anti-regression (content not lost) | Compare code-cell function defs MAIN vs BRANCH | `verify_spe` preserved 1→1; `create_ultimatum_game`+`create_stackelberg_game` (MAIN standalone) consolidated as `solve_ultimatum`+`build_three_player_entry_game` (BRANCH unified, improved semantics). **Net code +63 lines (504→567) = enrichment, not loss** ✓ |
| F2 | C.1 (no voluntary error) | `grep raise NotImplementedError\|assert False\|1/0` on code cells | **0 violations**. Stubs use `pass`/`return`/TODO per convention; comment explicitly documents C.1-safe (`# ces stubs utilisent uniquement pass / return / commentaires TODO etudiant`) ✓ |
| F3 | Execution (exec_count + outputs + errors) | Count per code cell | 13 code cells ; **execution_count=null: 0** ; **13/13 with outputs** ; **0 errors** = EXEC_PROVED ✓ |
| F4 | Content preservation (multiset-diff) | `Counter` of code source lines MAIN vs BRANCH | Function defs preserved/renamed (see F1) ; total non-empty code lines 504→567 (+63, enrichissement) ✓ |
| F5 | #2161 conformité (≥3 exos + hints + subsections) | Count subsections + hints + stubs | 3 subsections `### 8.N` ✓ ; 9 graduated hints (3/exo) ✓ ; 3 unified stubs ✓ ✓ |
| F6 | Cell count | `len(cells)` | 34→30 (4 standalone fragment cells consolidated, coherent with body's stated intent) ✓ |

## Stubs (C.1-safe, firsthand)

The unified code cell (branch cell 26) contains:
```python
def solve_ultimatum(offers=None, total: int = 10) -> Dict[str, Any]:
    return {...}   # neutral default, student replaces with SPE computation
def build_three_player_entry_game() -> ExtensiveFormGame:
    return game    # neutral default
def verify_spe(game, ...):
    return {...}   # neutral default
```
Stubs return neutral placeholder values (0/empty/False) that the student must replace — conformant with C.1 (no `raise NotImplementedError`/`assert False`/`1/0`).

## Bot-review guidance

Verdict `MERGEABLE (EXEC_PROVED)` — the −238 deletions are fragment-cell consolidation (content preserved + enriched, F1+F4 proven firsthand), not content loss. C.1 clean (F2), execution proven (F3: 0 null exec_count, 13/13 outputs, 0 errors), #2161 satisfied (F5: 3 exos + 9 hints + subsections). Same C411-L3 pattern as the merged GT-5/SC-7/GT-3 exo-stub PRs. Safe to merge.

## Rules referenced

- `../../../.claude/rules/three-exercises-per-notebook.md` (#2161 ≥3 exos + hints + subsections)
- `../../../.claude/rules/exercise-example-labeling.md` (content-based: stubs = exercises, classified by content)
- `../../../.claude/rules/notebook-conventions.md` (C.1 no voluntary error, C.2 outputs)
- `../../../.claude/rules/anti-regression.md` (F1: deletions = fragment consolidation, content preserved+enriched, no pedagogical loss)

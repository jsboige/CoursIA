# §D Tool Forensic — PR #6056 (count_exercises scanner fix, Bug 4)

- **PR**: #6056 — `fix(notebook-tools): count_exercises broadened pass-2 detects print/display exercise stub markers (#6051 Bug 4)`
- **Author**: po-2024 (lane `myia-po-2024:CoursIA`)
- **Reviewer**: po-2023 (lane `myia-po-2023:CoursIA`) — cross-owner
- **Date**: 2026-07-11
- **Files**: `scripts/notebook_tools/count_exercises.py` (+21/−1), `scripts/notebook_tools/tests/test_count_exercises.py` (+39)
- **Base**: `origin/main` (fresh, two-dot diff `origin/main..origin/fix/count-exercises-docstring-stubs-2161`)
- **Verdict**: **MERGEABLE**

## Scope

Bug 4 of the `count_exercises` scanner (#6051): pass-2 only flagged a code cell as an exercise when `_code_cell_mentions_exercise` found the exercise word in a full-line `#`/`//`/`--` comment. A stub whose only "exercice" reference lives in a `print("Exercice...")` / `display("Exercice 2...")` marker — or a `# Partie N`/`# Etape` scaffold with no "exercice" word in any comment — escaped detection. Canonical case: `SC-26-Final-Project.ipynb` Parties 2/3/4 (real student stubs) reported 0; reality 3.

## §D tool forensic (independent, two-dot byte-identity)

| # | Criterion | Method | Result |
|---|-----------|--------|--------|
| D1 | Fix logic minimal + sound | Read two-dot diff of `count_exercises.py` | Mention check changed from `(comment-aware)` to `(comment-aware) OR (full-source EXERCISE_WORD_RE)`. `_is_stub_code` gate **preserved** (the AND). `paired_code_indices` exclusion **unchanged`. An `or` branch only ADDS detections → **monotonic non-decrease by construction** ✓ |
| D2 | Test suite green | `pytest scripts/notebook_tools/tests/test_count_exercises.py -q` | **36 passed** (35 original + 1 new regression `test_stub_with_print_exercice_marker_no_comment_is_counted`) ✓ |
| D3 | Canonical case proven firsthand | Run scanner on `SC-26-Final-Project.ipynb` on BOTH main and branch | **MAIN = 0** (under-counted, bug confirmed) ; **BRANCH = 3** (fixed) ; **delta +3 exactly as body claims** ✓ |
| D4 | No regression on reviewer's turf (.NET) | Run scanner on Sudoku-0/1/10-Csharp.ipynb (my partition) | Sudoku-0=1, Sudoku-1=4, Sudoku-10=7 — stable, no spurious inflation on .NET families ✓ |
| D5 | Monotonicity (corpus) | By-construction proof + body's 921-nb claim | Construction is airtight: `or` widening + `_is_stub_code` gate + pairing-unchanged ⇒ counts can only stay equal or rise. Body reports 0 DECREASED / 14 INCREASED across 921 nb. Full corpus re-run is the gold standard but the structural argument is mathematically sound ✓ |

## Fix diff (essence)

```python
# Before (pass-2 mention check):
if _code_cell_mentions_exercise(source) and _is_stub_code(source):

# After:
mentions_exercise = (
    _code_cell_mentions_exercise(source)           # layer 1: comment-aware (unchanged)
    or bool(EXERCISE_WORD_RE.search(source))       # layer 2 NEW: full-source FR
    or bool(EXERCISE_WORD_EN_RE.search(source))    # layer 2 NEW: full-source EN
)
if mentions_exercise and _is_stub_code(source):    # _is_stub_code gate PRESERVED
```

Layer (2) is safe because pass-2 still requires `_is_stub_code`, so a complete solution mentioning "exercice" in prose is never counted. `paired_code_indices` (built in the unchanged pairing pass) already excludes header-paired stubs, so this only adds genuinely-unpaired stubs.

## Honesty note

- The by-construction monotonicity argument (D1+D5) is rigorous: widening a disjunct while preserving the gate and the pairing exclusion cannot decrease any notebook's count. I did NOT re-run the full 921-nb corpus (expensive; `check_docs_links` already timed out at 2 min on this machine) — I relied on the construction proof + the canonical-case firsthand verification (D3) + a .NET spot-check (D4). If a merge-blocker wants gold-standard certainty, a full corpus diff (main vs branch counts) is the remaining check; my assessment is the fix is safe to merge as-is.
- D3 is the strongest firsthand evidence: I swapped the working-tree file to main's version, ran the CLI on SC-26 (got 0), restored the branch version (got 3), confirmed 0-diff restore. The delta is real and exactly matches the body.
- po-2024's body is honest about scope: this is a **partial** fix (Bug 4 only); Bugs 1/2/3 (grouped markdown headers, section-pairing theft, docstring stubs) remain open and require a pairing/section refactor.

## Bot-review guidance

Verdict `MERGEABLE` — minimal, well-reasoned fix to the CI-gate scanner (#2161). Test suite green (36), canonical case proven firsthand (0→3), no regression signal on .NET turf, monotonic by construction. The fix is correctly scoped as partial (Bug 4) with Bugs 1/2/3 honestly deferred. Safe to merge.

## Rules referenced

- `../../../.claude/rules/three-exercises-per-notebook.md` (the scanner is the CI gate for this convention)
- `../../../.claude/rules/exercise-example-labeling.md` (content-based exercise detection)
- `../../../.claude/rules/anti-regression.md` (scanner change = +21/−1, tests added, no logic removed)
- `../../../.claude/rules/pr-review-discipline.md` (§F audit reassessment — methodology + cells verified firsthand)

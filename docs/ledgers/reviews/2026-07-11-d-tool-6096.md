# §D tool forensic review — PR #6096 (AutonomousProver wall-clock cap)

**Reviewer**: po-2024 (cross-lane; prover harness = po-2026 domain, 0 prior reviews).
**Date**: 2026-07-11 (c.30).
**Author**: jsboige (coordinator push, See #1453 forensic c.316).
**Verdict**: **MERGEABLE** — D1-D4 PASS firsthand. One non-blocking body/scope observation (Issue #2 diagnosed but not fixed).

## PR scope

`fix(prover): cap autonomous wall-clock budget to prevent 5h+ zai stalls (See #1453)`.
Files: `agent_tests/prover/provers.py` (+76), `agent_tests/tests/test_prover_guards.py` (+14). +89/-1 total.

Headline problem (from BG-prover trace forensic): `AutonomousProver` sessions burned 5.85h (`autonomous_custom_Basic_L308_zai` = 21084s, 10 iterations, sorry_delta=0) when the `zai` provider stalled, because the autonomous loop had **no session-level wall-clock cap** (only per-call `agent_timeout_s=900`).

## F-checks (firsthand)

### D1 — fix logic SOUND (PASS)
- New param `workflow_timeout_s: Optional[float] = None` on `AutonomousProver.prove_sorry` (provers.py:1019). Default None → `_AUTONOMOUS_CEILING_S = int(os.environ.get("PROVER_AUTONOMOUS_TIMEOUT_S", "2700"))` (45 min, matches MultiAgentSorryProver `CEILING_S` at provers.py:662).
- `_wallclock_cap = CEILING_S if workflow_timeout_s is None else min(workflow_timeout_s, CEILING_S)` — caller can LOWER the cap, not raise it; env var can raise CEILING for interactive runs.
- Per-iteration check (top of `_run_session` loop): `reasoning_s = elapsed - _build_credit_s; if reasoning_s > _wallclock_cap: break`. Reasoning time EXCLUDES compile time (credited via wrapping `tactic_tools.compile()`), matching the multi-agent build-credit pattern at provers.py:688-692.
- Edge cases verified: `workflow_timeout_s=0` → `min(0, 2700)=0` → `if 0 > 0` False → disabled (matches "Pass 0 to disable" intent). Env `=0` → same. Faithful mirror of the canonical multi-agent supervisor (provers.py:652-734, `PER_UNIT_S=CEILING_S=2700`, reasoning check at L711).

### D2 — guard tests firsthand (PASS)
Ran `tests/test_prover_guards.py` on the PR branch: **135 passed / 2 failed**.
- New test `test_autonomous_prover_accepts_workflow_timeout` (asserts param present + default None) → PASS (among the 135).
- The 2 fails (`test_path_constants_resolve_to_active_workspace`, `test_candidates_list_prefers_workspace_relative_entry`) are **PRE-EXISTING on main** (po-2026 c.313 revert-verified) = bug #5891 `CoursIA` vs `CoursIA-2` workspace path constants, **UNRELATED to #6096**. Collection 137 = 136 (post-#6081) + 1 new.

### D3 — does it prevent the 5h stall? (PASS)
The pathology was unconstrained iterations × zai 600s/call. The 45-min hard cap terminates regardless of iteration count or build status. **Part 1 alone solves the headline 5h+ stall** — the cap is the robust safety net; the DELTA0_HARDCAP refinement (Issue #2) is orthogonal.

### D4 — minimal, no regression (PASS)
Pure additive: new optional param (default None preserves existing call sites), new optional per-iteration check (gated `if _wallclock_cap > 0`), new build-credit accumulator. No existing behavior changed when cap not triggered. 0 deletions of logic (the -1 is a line-split).

## Non-blocking observation — body/scope mismatch on Issue #2

The body's "Root cause (two-part)" table diagnoses **two** issues:
1. No wall-clock cap (FIXED in this PR).
2. `DELTA0_HARDCAP=6` stagnation guard fires only on successful builds (`_record_sorry_count` at tools.py:1007), so BUILD-FAIL iterations never increment `_consecutive_delta0` → guard invisible to the most common pathology.

The diff implements **only Issue #1**. Issue #2's fix (increment `_consecutive_delta0` on BUILD-FAIL, or a parallel build-fail stagnation counter) is **not in the diff**. This is not a false claim — the body's "## Fix" section describes only the wall-clock cap — but a reader of the "two-part root cause" expects both to be addressed. **Recommend**: either (a) a one-line body clarification that Issue #2's fix is deferred to a follow-up, or (b) file a follow-up issue. Non-blocking: Part 1 is sufficient for the headline problem, and Issue #2 is a defense-in-depth refinement.

## Why MERGEABLE despite the observation

- Part 1 (wall-clock cap) is sound, tested, and fully solves the reported 5.85h stall.
- The cap is a strictly-stronger safety net than the Issue #2 stagnation guard (it bounds wall-clock unconditionally; the guard bounds stagnation). Shipping Part 1 now is a strict improvement.
- No regression, no anti-regression concern (prover harness tooling, not a Lean proof).

See #6096, #1453
